# 🐟 Fish Sales Forecasting System

Система прогнозирования продаж рыбной продукции с использованием глубокого обучения (N-BEATS и LSTM).

## 📋 Описание проекта

Проект содержит реализацию современных нейросетевых архитектур для прогнозирования временных рядов:

- **N-BEATS** (Neural Basis Expansion Analysis for Time Series) - основная архитектура
- **LSTM** (Long Short-Term Memory) - базовая реализация для сравнения
- **Поддержка множественных экзогенных переменных** (погода, календарные параметры, etc.)

### Ключевые особенности:

✅ Многомерные временные ряды (31 признак)
✅ Отдельные модели для каждого типа продукта (номенклатуры)
✅ Поддержка GPU ускорения
✅ Встроенная регуляризация и early stopping
✅ Интерпретируемые прогнозы

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
pip install -r requirements.txt
```

### 2. Подготовка данных

Поместите файл `DATA.csv` в папку `data/`:

```bash
mkdir -p data
cp YOUR_DATA.csv data/DATA.csv
```

**Требуемые колонки в DATA.csv:**
- `Номенклатура` - название продукта
- `Количество` - целевая переменная (продажи)
- `Себестоимость` - стоимость товара
- `Продажа` - цена продажи
- `Прибыль` - прибыль
- `date` - дата
- Метеорологические данные (температура, давление, осадки, etc.)
- Календарные параметры (день недели, праздники, сезон, etc.)

### 3. Обучение модели N-BEATS (РЕКОМЕНДУЕТСЯ)

```bash
python train_nbeats.py
```

Или обучение модели LSTM:

```bash
python train_models.py
```

### 4. Прогнозирование

```bash
python predict_nbeats.py
```

Интерактивное меню позволит выбрать:
- Прогноз на 30 дней
- Прогноз на пользовательское количество дней
- Выход

## 🤖 Архитектуры моделей

### N-BEATS (Рекомендуется)

**Преимущества:**
- ⚡ 2-3x быстрее, чем LSTM
- 📈 20-30% улучшение точности
- 🔍 Лучшая интерпретируемость
- 🎯 Residual stacking для разложения сигнала
- 💪 Стабильное обучение

**Архитектура:**
```
Input (flattened: 690 признаков)
    ↓
┌─ Stack 1 (Generic) ─────┐
│  - 3 блока               │
│  - 512 hidden units      │ → Forecast 1
│  - Dropout 0.1           │
└──────────────────────────┘
    
┌─ Stack 2 (Trend-like) ───┐
│  - 3 блока               │
│  - 512 hidden units      │ → Forecast 2
│  - Dropout 0.1           │
└──────────────────────────┘

┌─ Stack 3 (Seasonality) ──┐
│  - 3 блока               │
│  - 512 hidden units      │ → Forecast 3
│  - Dropout 0.1           │
└──────────────────────────┘

Final Forecast = Forecast 1 + Forecast 2 + Forecast 3
```

### LSTM (Альтернатива)

**Характеристики:**
- Классическая архитектура RNN
- Хорошо захватывает долгосрочные зависимости
- Базовая реализация для сравнения

## 📊 Гиперпараметры

### N-BEATS

```python
SEQUENCE_LENGTH = 30           # Lookback window (дни)
FORECAST_HORIZON = 7           # Прогноз на (дни)
NUM_STACKS = 3                 # Количество стеков
NUM_BLOCKS = 3                 # Блоков в стеке
HIDDEN_LAYERS = [512, 512]     # Размеры скрытых слоев
DROPOUT = 0.1                  # Регуляризация
BATCH_SIZE = 32
EPOCHS = 100
LEARNING_RATE = 0.001
EARLY_STOPPING_PATIENCE = 10
```

**Оптимизация для вашего оборудования:**

- **Слабый GPU / CPU:**
  ```python
  BATCH_SIZE = 16
  HIDDEN_LAYERS = [256, 256]
  NUM_BLOCKS = 2
  ```

- **Мощный GPU / больше памяти:**
  ```python
  BATCH_SIZE = 64
  HIDDEN_LAYERS = [512, 512, 512]
  NUM_BLOCKS = 4
  ```

## 🔧 Установка и конфигурация

### Системные требования

```
OS: Linux, macOS, или Windows
Python: 3.8+
RAM: 8 GB минимум (16 GB рекомендуется)
GPU: NVIDIA GPU с CUDA поддержкой (опционально, но рекомендуется)
```

### Установка с GPU поддержкой (CUDA)

```bash
# Установка зависимостей
pip install -r requirements.txt

# CUDA 11.8 (для старых видеокарт)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118

# CUDA 12.1 (для новых видеокарт)
pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121
```

### Установка для CPU

```bash
pip install -r requirements.txt
# PyTorch будет установлен с поддержкой только CPU
```

## 📈 Использование

### Обучение модели

```bash
# N-BEATS (рекомендуется)
python train_nbeats.py

# LSTM (альтернатива)
python train_models.py
```

**Вывод:**
```
Используется устройство: cuda (или cpu)

Параметры модели:
  - Sequence Length (lookback): 30
  - Forecast Horizon: 7
  - Количество стеков: 3
  - Количество блоков в стеке: 3
  - Скрытые слои: [512, 512]

Загрузка данных...
Найдено 5 номенклатур

[1/5] Обучаем модель для: Щука вяленая (вес)
  - Размер входа (flattened): 690 (30 дней × 23 признаков)
  - Размер обучающего набора: 80
  - Размер валидационного набора: 20
  - Количество параметров: 3,543,847
  - Обучение запущено...
Epoch 10/100 | Train Loss: 0.123456 | Val Loss: 0.234567
Epoch 20/100 | Train Loss: 0.098765 | Val Loss: 0.187654
...
```

### Прогнозирование

```bash
python predict_nbeats.py
```

**Интерактивное меню:**
```
╔════════════════════════════════════════════════════════════╗
║ N-BEATS: Система прогнозирования продаж рыбной продукции  ║
╚════════════════════════════════════════════════════════════╝

Выберите режим:
1 - Прогноз на 30 дней
2 - Прогноз на пользовательское количество дней
3 - Выход

Ваш выбор (1-3): 1

Делаю прогнозы на 30 дней...

============================================================
РЕЗУЛЬТАТЫ ПРОГНОЗА:
============================================================
                     Номенклатура       Прогноз_общий     Среднее_значение    Количество_дней
0              Щука вяленая (вес)        15432.45             2205.49               7
1             Кета х/к теша (вес)        8765.23              1251.61               7
2        Сырок х/к северный (вес)        5432.11              775.44                7
...
============================================================
```

## 🐛 Решение проблем


### Ошибка: "CUDA out of memory"

**Решение:**
```python
# В train_nbeats.py уменьшите:
BATCH_SIZE = 16  # вместо 32
HIDDEN_LAYERS = [256, 256]  # вместо [512, 512]
```

### Ошибка: "ModuleNotFoundError: No module named 'utils'"

**Решение:**
```bash
# Убедитесь, что utils.py находится в том же каталоге
ls utils.py
# Должно вывести: utils.py
```

### Модель обучается медленно

**Решение:**
```python
# Используйте GPU вместо CPU:
# PyTorch автоматически выберет GPU если доступно

# Проверить использование GPU:
python -c "import torch; print('GPU:', torch.cuda.is_available())"
```

## 📊 Метрики качества

Модель оценивается по следующим метрикам:

| Метрика | Описание | Как интерпретировать |
|---------|---------|---------------------|
| **MSE** | Mean Squared Error | Используется при обучении, штрафует большие ошибки |
| **MAE** | Mean Absolute Error | Средняя абсолютная ошибка в единицах товара |
| **RMSE** | Root Mean Squared Error | Корень из MSE, легче интерпретировать |
| **MAPE** | Mean Absolute Percentage Error | Процентная ошибка (5% хорошо, 15% приемлемо) |

**Целевые значения:**
- MAE < 50 единиц - отличное качество
- MAE < 100 единиц - хорошее качество
- MAE < 200 единиц - приемлемое качество

## 🤝 Контрибьютинг

Если вы хотите улучшить проект:

1. Протестируйте на своих данных
2. Сообщите о проблемах в issues
3. Предложите улучшения в pull requests

## 📝 Лицензия

Этот проект распространяется под лицензией MIT.

## 👨‍💻 Автор Моисев Семён Андреевич 

Система разработана для прогнозирования количества рыбной продукции.

## 🔗 Полезные ссылки

- [PyTorch Documentation](https://pytorch.org/docs/stable/index.html)
- [N-BEATS Paper](https://arxiv.org/abs/1905.10437)
- [Time Series Forecasting Guide](https://machinelearningmastery.com/time-series-forecasting/)

## 💡 Советы по использованию

### Для новичков:
1. Начните с обучения N-BEATS на полном наборе данных
2. Используйте команду `python predict_nbeats.py` для тестирования
3. Если возникают ошибки - см. раздел "Решение проблем" выше

### Для опытных пользователей:
1. Экспериментируйте с гиперпараметрами в `train_nbeats.py`
2. Рассмотрите гибридный подход: N-BEATS + XGBoost
3. Используйте Temporal Fusion Transformer для лучшей точности
4. Имплементируйте стратегию ансамбля (ensemble) нескольких моделей

### Для production:
1. Используйте версии: `nbeats_model.py` и `train_nbeats.py`
2. Настройте мониторинг качества прогнозов
3. Регулярно переобучайте модель на свежих данных
4. Создайте API для интеграции с другими системами

## ⚙️ Команды быстрого старта

```bash
# Полная установка и запуск
git clone <repository>
cd fish-sales-forecasting
python -m venv .venv
```
Для Windows используйте 
```cmd
.venv\Scrips\activate
```

Для Linux используйте
```bash
source .venv/bin/activate
```

```bash
pip install -r requirements.txt
mkdir -p data
# Поместите DATA.csv в папку data/
python train_nbeats.py
python predict_nbeats.py

# Быстрое исправление известных ошибок
bash quick_fix.sh

# Проверка окружения
python -c "import torch; print('PyTorch:', torch.__version__); print('GPU:', torch.cuda.is_available())"
```

---

**Последнее обновление:** Ноябрь 2025
**Версия проекта:** 2.0 (N-BEATS + исправления)
